import { ComingSoonFallback } from "@/components/ComingSoonFallback";

export default function SupplierPage() {
  return (
    <ComingSoonFallback
      featureName="Supplier Intelligence"
      businessObjective="contract coverage validation, reliability scoring, and supplier performance routing"
    />
  );
}
